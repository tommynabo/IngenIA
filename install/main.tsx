
import React from 'react';
import ReactDOM from 'react-dom/client';
import InstallPage from './Page.tsx';

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <InstallPage />
    </React.StrictMode>
);
